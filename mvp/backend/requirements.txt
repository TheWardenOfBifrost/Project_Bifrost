# --- Core framework ---
fastapi==0.115.2
uvicorn[standard]==0.30.6   # inkluderer watchfiles m.m.

# --- DB layer ---
sqlmodel==0.0.22            # inkluderer SQLAlchemy 2.x

# --- Data models & validation ---
pydantic[email]==2.9.2      # EmailStr support
# email-validator følger med gennem pydantic[email] og behøver ikke angives separat

# --- Utils (nice to have / brugt) ---
python-multipart==0.0.9     # hvis vi uploader form-data senere
